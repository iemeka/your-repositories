
three rows
1 - logo . todo list left
2 - center items -  image, [ Full Name, Bio, ] ff-n0, fll-num repo numver - https://api.github.com/users/iemeka
3 - main content - github image at background -  
  - https://api.github.com/users/iemeka/repos?type=all&sort=updated
  - html-url - link - bottom left
  - name
  - description [cut with ...]
  - stargazers_count
  - watchers_count

  design - AYoKA

fetch and display data.
Todo list  - slide in from left
- Redux for todo list - check totorial now - clionton
- gh-pages
- Netlify Identity login - signup - logout (bottom right of page.)
  - get gitusername
  - save in redux - use to make requests - make request first before page loads

design
animation
dark mode
if git user name fetches no data - return with error messages


normally
login first - so we do Netlify Identity first
  - host on netlify
  - implement authentication  - react-netlify-Identity-gotrue
  login and logout working.
  todolist - done.

design. next.



  // const seeObject = useSelector((state) => state.userObject);
  // const dispatch = useDispatch();
  // <button onClick={() => dispatch(updateUserObject("dd"))}>update object</button>
  //       {`${seeObject}`}


  // import { useDispatch, useSelector } from "react-redux";
// import { getUserObject ,updateUserObject} from "./redux/action";

incorrect git user name - check

todo list not persistent

todo list - mark as done. 
